#include <iostream>
#include <vector>

using namespace std;

void comb(vector<int>& arr, int num, int target, int idx, vector<vector<int>>& res, vector<int>& temp){
    
}

vector<vector<int>> combinationSum3(int k, int n) {
    vector<vector<int>> res;
    vector<int> temp;

    vector<int> arr = {1,2,3,4,5,6,7,8,9};
    
    comb(arr, k, n, 0, res, temp);
}