#include <bits/stdc++.h>
using namespace std;

int main(){
    int t;
    cin >> t;

    while(t--){
        int n;
        cin >> n;

        vector<pair<string, int>> res;
        unordered_map<string, vector<int>> map;

        for(int i = 0; i < n; i++){
            string f;
            int p;
            cin >> f >> p;

            for()
        }
    }
}