#include <bits/stdc++.h>
using namespace std;

int donation(vector<int>& b, vector<int>& m, vector<int>& l) {
    unordered_map<int, vector<int>> map;
    for (int i = 1; i < b.size(); i++) {
        if )
        map[m[i - 1]].push_back(i);
    }
}